{
  "name": "@agrifur/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "worker": "bun src/worker-only.ts",
    "preview": "cd ../.. && bun run preview",
    "seed": "bun src/scripts/seed.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bun build src/index.ts --outdir dist --target bun --sourcemap=external"
  },    "dependencies": {
      "express": "^5.1.0",
    "mqtt": "^5.15.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@types/express": "^5.0.3",
    "@types/node": "^22.15.30",
    "aedes": "^1.1.2",
    "typescript": "^5.8.3"
  }
}
